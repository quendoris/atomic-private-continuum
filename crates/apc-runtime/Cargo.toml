[package]
name = "apc-runtime"
version = "0.0.1"
edition = "2021"
license = "GPL-3.0-or-later"
description = "Platform-neutral runtime orchestration for Atomic Private Continuum"
publish = false

[dependencies]
apc-core = { path = "../apc-core" }
apc-sync = { path = "../apc-sync" }
apc-transport-github = { path = "../apc-transport-github" }

[dev-dependencies]
apc-crypto = { path = "../apc-crypto" }
apc-storage-fs = { path = "../apc-storage-fs" }

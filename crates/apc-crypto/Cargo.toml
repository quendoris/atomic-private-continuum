[package]
name = "apc-crypto"
version = "0.0.1"
edition = "2021"
license = "GPL-3.0-or-later"
description = "Authenticated protection primitives for Atomic Private Continuum"
publish = false

[dependencies]
chacha20poly1305 = { version = "0.11", features = ["zeroize"] }
zeroize = "1"

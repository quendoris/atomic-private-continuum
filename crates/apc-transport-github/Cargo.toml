[package]
name = "apc-transport-github"
version = "0.0.1"
edition = "2021"
license = "GPL-3.0-or-later"
description = "GitHub opaque synchronization transport adapter for Atomic Private Continuum"
publish = false

[dependencies]
apc-sync = { path = "../apc-sync" }
sha2 = "0.10"
